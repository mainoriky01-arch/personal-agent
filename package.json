{
  "name": "personal-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Personal Agent — behavioural agent monorepo (spec §22.5)",
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
